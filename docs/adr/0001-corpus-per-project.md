# Corpus 每项目一份，而非个人全局

Bug Corpus 的终目标是发现跨项目的「AI 开发失败模式」，但 Case 数据存放在每个消费方项目自己的仓库里（根目录 `bug-corpus/`，随项目进 git），而不是作者个人维护的全局语料库。理由：skill 会分发给很多团队，数据落在 bug 发生的上下文中才能被该团队共享，也不会把 A 项目的隐私写进 B 项目；跨项目规律属于蒸馏层，将来由聚合工具处理，聚合时项目身份直接从仓库本身读取（因此 Case 也不设 `project.name` 字段）。代价：作者的跨项目复利要等聚合工具出现才能兑现。
